// Copyright ${year} ${name_of_copyright_owner}
// ${license}
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


`ifndef __UVMA_${name_uppercase}_DP_IF_CHKR_SV__
`define __UVMA_${name_uppercase}_DP_IF_CHKR_SV__


/**
 * Encapsulates assertions targeting uvma_${name}_dp_if.
 * This module must be bound to an interface in a test bench.
 */
module uvma_${name}_dp_if_chkr (
   uvma_${name}_dp_if  ${name}_if
);

   // TODO Add assertions to uvma_${name}_dp_if_chkr

endmodule : uvma_${name}_dp_if_chkr


`endif // __UVMA_${name_uppercase}_DP_IF_CHKR_SV__
